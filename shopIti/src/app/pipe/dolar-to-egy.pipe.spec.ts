import { DolarToEgyPipe } from './dolar-to-egy.pipe';

describe('DolarToEgyPipe', () => {
  it('create an instance', () => {
    const pipe = new DolarToEgyPipe();
    expect(pipe).toBeTruthy();
  });
});
